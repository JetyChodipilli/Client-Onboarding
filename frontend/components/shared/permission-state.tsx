import { LockKeyhole } from "lucide-react";
export function PermissionState(){return <div className="panel flex min-h-64 flex-col items-center justify-center px-6 text-center"><div className="grid size-11 place-items-center rounded-xl bg-[hsl(var(--warning-soft))] text-[hsl(var(--warning))]"><LockKeyhole className="size-5"/></div><h2 className="mt-4 font-semibold">Permission required</h2><p className="mt-1 max-w-md text-sm leading-6 text-[hsl(var(--muted-foreground))]">Your current role does not allow access to this area. Ask an organization administrator if you need it.</p></div>}
