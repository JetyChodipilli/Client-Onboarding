import { AuthShell } from "@/components/auth/auth-shell";import { AcceptInvitationForm } from "@/features/auth/accept-invitation-form";export default async function AcceptInvitationPage({searchParams}:{searchParams:Promise<{token?:string}>}){const{token=""}=await searchParams;return <AuthShell eyebrow="Workspace invitation" title="Join your organization" description="Invitation links are securely scoped to one organization and assigned roles."><AcceptInvitationForm token={token}/></AuthShell>}
