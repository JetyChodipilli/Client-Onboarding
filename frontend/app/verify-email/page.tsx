import { AuthShell } from "@/components/auth/auth-shell";import { VerifyEmailCard } from "@/features/auth/verify-email-card";export default async function VerifyEmailPage({searchParams}:{searchParams:Promise<{token?:string}>}){const{token=""}=await searchParams;return <AuthShell eyebrow="Email verification" title="Confirm your identity" description="Verification links are single-use and expire automatically."><VerifyEmailCard token={token}/></AuthShell>}
