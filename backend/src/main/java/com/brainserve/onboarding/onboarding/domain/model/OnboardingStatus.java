package com.brainserve.onboarding.onboarding.domain.model;

public enum OnboardingStatus {
    DRAFT, INVITED, IN_PROGRESS, AWAITING_INTERNAL_REVIEW, NEEDS_REVISION, APPROVED, COMPLETED, PAUSED, EXPIRED, CANCELLED
}
