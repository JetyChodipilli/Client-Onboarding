package com.brainserve.onboarding.reminders.api.response;import com.brainserve.onboarding.reminders.domain.model.ScheduledReminderStatus;import java.time.Instant;import java.util.UUID;public record ScheduledReminderResponse(UUID id,UUID reminderPolicyId,UUID projectId,UUID onboardingId,UUID stepId,ScheduledReminderStatus status,Instant nextRunAt,int sentCount,Instant lastSentAt,String suppressionReason,long version){}
