server:
  forward-headers-strategy: native

app:
  security:
    require-explicit-secrets: true
    secure-cookies: true
    public-api-docs: false
  bootstrap:
    enabled: false
  assets:
    storage:
      enabled: true
    scan:
      enabled: true
  contracts:
    storage:
      enabled: true
