CREATE SCHEMA IF NOT EXISTS client_onboarding;

COMMENT ON SCHEMA client_onboarding IS
    'Client Onboarding Platform application schema. Business tables are added only by their implementation phases.';
