# ADR-0005: S3-Compatible Object Storage

**Status:** Accepted

## Decision

Future asset and signed-document storage uses an S3-compatible object store behind authorized short-lived upload/download flows. Bucket URLs are not public application URLs.
